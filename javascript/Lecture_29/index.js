/*

const { normalize } = require("path");

const prompt = require("prompt-sync")();


// Question no. 1, 2, 3
runagain = true;

while (runagain) {

  let age = prompt(" Please enter your age. ");

  age = Number.parseInt(age);

  let minimum_age = 18;

  minimum_age = Number.parseInt(minimum_age);
  
  if (age <= 0) {
    console.error("Your entered input is not valid");
    break;
  };
  

  alert(age <= 0 ? "Please enter valid input" : age < minimum_age ? "Not eligible" : "You can drive.")

  runagain = confirm(" Do you want to renter your age ? ")

console.log( confirm == true ? "You pressed ok" : "You pressed cancel" );

  

}

//Question no. 4

let number = prompt(" Enter your number ");

number = Number.parseInt(number);

if( number > 4 ){
  location.href = "https://google.com"
}

//Question no. 5

*/